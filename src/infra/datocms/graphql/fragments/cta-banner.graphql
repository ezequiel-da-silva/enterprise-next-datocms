# Fragmento do bloco CTA Banner — incluir em `page-by-slug.graphql` (blocks union).
#import "./text-header.graphql"
#import "./specialized-image-blocks.graphql"

fragment CtaBannerBlockFields on CtaBannerRecord {
  id
  textHeaderSection {
    ...TextHeaderBlockFragment
  }
  hasEyebrow
  eyebrow
  advancedOptions
  variant
  bgTheme
  hasImage
  buttons {
    __typename
    id
    ctaLabel
    typeContent
    externalLink
    openInNewTab
    ctaLinkAria
    internalLinkPage {
      __typename
      slug
    }
    internalLinkPost {
      __typename
      postSlug
    }
    internalLinkCategory {
      __typename
      categorySlug
    }
    internalLinkAuthor {
      __typename
      authorSlug
    }
  }
  imageBanner {
    ...BannerImageBlockFields
  }
}
